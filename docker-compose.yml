# 使用 Node 基础镜像
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制后端依赖并安装
COPY server/package*.json ./server/
RUN cd server && npm install --production

# 复制所有代码
COPY . .

# 暴露端口
EXPOSE 3000

# 启动命令
CMD ["node", "server/index.js"]